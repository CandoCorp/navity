#
# Copyright © 2016-2019 The Thingsboard Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: zookeeper
  namespace: navity
spec:
  template:
    metadata:
      labels:
        app: zookeeper
    spec:
      containers:
      - name: server
        imagePullPolicy: Always
        image: zookeeper:3.5
        ports:
        - containerPort: 2181
        readinessProbe:
          periodSeconds: 5
          tcpSocket:
            port: 2181
        livenessProbe:
          periodSeconds: 5
          tcpSocket:
            port: 2181
        env:
        - name: ZOO_MY_ID
          value: "1"
        - name: ZOO_SERVERS
          value: "server.1=0.0.0.0:2888:3888;0.0.0.0:2181"
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: zookeeper
  namespace: navity
spec:
  type: ClusterIP
  selector:
    app: zookeeper
  ports:
  - name: zk-port
    port: 2181
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: navity-kafka
  namespace: navity
spec:
  template:
    metadata:
      labels:
        app: navity-kafka
    spec:
      containers:
      - name: server
        imagePullPolicy: Always
        image: wurstmeister/kafka:2.12-2.2.1
        ports:
        - containerPort: 9092
        readinessProbe:
          periodSeconds: 20
          tcpSocket:
            port: 9092
        livenessProbe:
          periodSeconds: 5
          tcpSocket:
            port: 9092
        env:
        - name: KAFKA_ZOOKEEPER_CONNECT
          value: "zookeeper:2181"
        - name: KAFKA_LISTENERS
          value: "INSIDE://:9093,OUTSIDE://:9092"
        - name: KAFKA_ADVERTISED_LISTENERS
          value: "INSIDE://:9093,OUTSIDE://tb-kafka:9092"
        - name: KAFKA_LISTENER_SECURITY_PROTOCOL_MAP
          value: "INSIDE:PLAINTEXT,OUTSIDE:PLAINTEXT"
        - name: KAFKA_INTER_BROKER_LISTENER_NAME
          value: "INSIDE"
        - name: KAFKA_CREATE_TOPICS
          value: "js.eval.requests:100:1:delete --config=retention.ms=60000 --config=segment.bytes=26214400 --config=retention.bytes=104857600,tb.transport.api.requests:30:1:delete --config=retention.ms=60000 --config=segment.bytes=26214400 --config=retention.bytes=104857600,tb.rule-engine:30:1:delete --config=retention.ms=60000 --config=segment.bytes=26214400 --config=retention.bytes=104857600"
        - name: KAFKA_AUTO_CREATE_TOPICS_ENABLE
          value: "false"
        - name: KAFKA_LOG_RETENTION_BYTES
          value: "1073741824"
        - name: KAFKA_LOG_SEGMENT_BYTES
          value: "268435456"
        - name: KAFKA_LOG_RETENTION_MS
          value: "300000"
        - name: KAFKA_LOG_CLEANUP_POLICY
          value: "delete"
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: navity-kafka
  namespace: navity
spec:
  type: ClusterIP
  selector:
    app: navity-kafka
  ports:
  - name: navity-kafka-port
    port: 9092
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: navity-redis
  namespace: navity
spec:
  template:
    metadata:
      labels:
        app: navity-redis
    spec:
      containers:
      - name: server
        imagePullPolicy: Always
        image: redis:4.0
        ports:
        - containerPort: 6379
        readinessProbe:
          periodSeconds: 5
          tcpSocket:
            port: 6379
        livenessProbe:
          periodSeconds: 5
          tcpSocket:
            port: 6379
        volumeMounts:
        - mountPath: /data
          name: redis-data
      volumes:
      - name: redis-data
        emptyDir: {}
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: navity-redis
  namespace: navity
spec:
  type: ClusterIP
  selector:
    app: navity-redis
  ports:
  - name: navity-redis-port
    port: 6379
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: navity-js-executor
  namespace: navity
spec:
  replicas: 20
  selector:
    matchLabels:
      app: navity-js-executor
  template:
    metadata:
      labels:
        app: navity-js-executor
    spec:
      containers:
      - name: server
        imagePullPolicy: Always
        image: navity/navity-js-executor:latest
        env:
        - name: REMOTE_JS_EVAL_REQUEST_TOPIC
          value: "js.eval.requests"
        - name: NAVITY_KAFKA_SERVERS
          value: "navity-kafka:9092"
        - name: LOGGER_LEVEL
          value: "info"
        - name: LOG_FOLDER
          value: "logs"
        - name: LOGGER_FILENAME
          value: "navity-js-executor-%DATE%.log"
        - name: DOCKER_MODE
          value: "true"
        - name: SCRIPT_BODY_TRACE_FREQUENCY
          value: "1000"
      restartPolicy: Always
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: navity-node
  namespace: navity
spec:
  replicas: 2
  selector:
    matchLabels:
      app: navity-node
  template:
    metadata:
      labels:
        app: navity-node
    spec:
      volumes:
        - name: navity-node-config
          configMap:
            name: navity-node-config
            items:
            - key: conf
              path:  navity.conf
            - key: logback
              path:  logback.xml
      containers:
      - name: server
        imagePullPolicy: Always
        image: navity/navity-node:latest
        ports:
        - containerPort: 8080
          name: http
        - containerPort: 9001
          name: rpc
        env:
        - name: RPC_HOST
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: CLUSTER_NODE_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: NAVITY_HOST
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: ZOOKEEPER_ENABLED
          value: "true"
        - name: ZOOKEEPER_URL
          value: "zookeeper:2181"
        - name: NAVITY_KAFKA_SERVERS
          value: "navity-kafka:9092"
        - name: JS_EVALUATOR
          value: "remote"
        - name: TRANSPORT_TYPE
          value: "remote"
        - name: CACHE_TYPE
          value: "redis"
        - name: REDIS_HOST
          value: "navity-redis"
        - name: HTTP_LOG_CONTROLLER_ERROR_STACK_TRACE
          value: "false"
        envFrom:
        - configMapRef:
            name: navity-node-db-config
        volumeMounts:
          - mountPath: /config
            name: navity-node-config
        livenessProbe:
          httpGet:
            path: /login
            port: http
          initialDelaySeconds: 120
          timeoutSeconds: 10
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: navity-node
  namespace: navity
spec:
  type: ClusterIP
  selector:
    app: navity-node
  ports:
  - port: 8080
    name: http
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: navity-mqtt-transport
  namespace: navity
spec:
  replicas: 2
  selector:
    matchLabels:
      app: navity-mqtt-transport
  template:
    metadata:
      labels:
        app: navity-mqtt-transport
    spec:
      volumes:
        - name: navity-mqtt-transport-config
          configMap:
            name: navity-mqtt-transport-config
            items:
            - key: conf
              path:  navity-mqtt-transport.conf
            - key: logback
              path:  logback.xml
      containers:
      - name: server
        imagePullPolicy: Always
        image: navity/navity-mqtt-transport:latest
        ports:
        - containerPort: 1883
          name: mqtt
        env:
        - name: CLUSTER_NODE_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: NAVITY_HOST
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: MQTT_BIND_ADDRESS
          value: "0.0.0.0"
        - name: MQTT_BIND_PORT
          value: "1883"
        - name: MQTT_TIMEOUT
          value: "10000"
        - name: NAVITY_KAFKA_SERVERS
          value: "navity-kafka:9092"
        volumeMounts:
          - mountPath: /config
            name: navity-mqtt-transport-config
        readinessProbe:
          periodSeconds: 20
          tcpSocket:
            port: 1883
        livenessProbe:
          periodSeconds: 20
          tcpSocket:
            port: 1883
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: navity-mqtt-transport
  namespace: navity
spec:
  type: LoadBalancer
  selector:
    app: navity-mqtt-transport
  ports:
  - port: 1883
    targetPort: 1883
    name: mqtt
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: navity-http-transport
  namespace: navity
spec:
  replicas: 2
  selector:
    matchLabels:
      app: navity-http-transport
  template:
    metadata:
      labels:
        app: navity-http-transport
    spec:
      volumes:
        - name: navity-http-transport-config
          configMap:
            name: navity-http-transport-config
            items:
            - key: conf
              path:  navity-http-transport.conf
            - key: logback
              path:  logback.xml
      containers:
      - name: server
        imagePullPolicy: Always
        image: navity/navity-http-transport:latest
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: CLUSTER_NODE_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: NAVITY_HOST
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: HTTP_BIND_ADDRESS
          value: "0.0.0.0"
        - name: HTTP_BIND_PORT
          value: "8080"
        - name: HTTP_REQUEST_TIMEOUT
          value: "60000"
        - name: NAVITY_KAFKA_SERVERS
          value: "navity-kafka:9092"
        volumeMounts:
          - mountPath: /config
            name: navity-http-transport-config
        readinessProbe:
          periodSeconds: 20
          tcpSocket:
            port: 8080
        livenessProbe:
          periodSeconds: 20
          tcpSocket:
            port: 8080
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: navity-http-transport
  namespace: navity
spec:
  type: ClusterIP
  selector:
    app: navity-http-transport
  ports:
  - port: 8080
    name: http
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: navity-coap-transport
  namespace: navity
spec:
  replicas: 2
  selector:
    matchLabels:
      app: navity-coap-transport
  template:
    metadata:
      labels:
        app: navity-coap-transport
    spec:
      volumes:
        - name: navity-coap-transport-config
          configMap:
            name: navity-coap-transport-config
            items:
            - key: conf
              path:  navity-coap-transport.conf
            - key: logback
              path:  logback.xml
      containers:
      - name: server
        imagePullPolicy: Always
        image: navity/navity-coap-transport:latest
        ports:
        - containerPort: 5683
          name: coap
          protocol: UDP
        env:
        - name: CLUSTER_NODE_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: NAVITY_HOST
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: COAP_BIND_ADDRESS
          value: "0.0.0.0"
        - name: COAP_BIND_PORT
          value: "5683"
        - name: COAP_TIMEOUT
          value: "10000"
        - name: NAVITY_KAFKA_SERVERS
          value: "navity-kafka:9092"
        volumeMounts:
          - mountPath: /config
            name: navity-coap-transport-config
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: navity-coap-transport
  namespace: navity
spec:
  type: LoadBalancer
  selector:
    app: navity-coap-transport
  ports:
  - port: 5683
    name: coap
    protocol: UDP
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: navity-web-ui
  namespace: navity
spec:
  replicas: 2
  selector:
    matchLabels:
      app: navity-web-ui
  template:
    metadata:
      labels:
        app: navity-web-ui
    spec:
      containers:
      - name: server
        imagePullPolicy: Always
        image: navity/navity-web-ui:latest
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: HTTP_BIND_ADDRESS
          value: "0.0.0.0"
        - name: HTTP_BIND_PORT
          value: "8080"
        - name: NAVITY_ENABLE_PROXY
          value: "false"
        - name: LOGGER_LEVEL
          value: "info"
        - name: LOG_FOLDER
          value: "logs"
        - name: LOGGER_FILENAME
          value: "navity-web-ui-%DATE%.log"
        - name: DOCKER_MODE
          value: "true"
        livenessProbe:
          httpGet:
            path: /index.html
            port: http
          initialDelaySeconds: 120
          timeoutSeconds: 10
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: navity-web-ui
  namespace: navity
spec:
  type: ClusterIP
  selector:
    app: navity-web-ui
  ports:
  - port: 8080
    name: http
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: navity-ingress
  namespace: navity
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
spec:
  rules:
  - http:
      paths:
      - path: /api/v1/.*
        backend:
          serviceName: navity-http-transport
          servicePort: 8080
      - path: /static/rulenode/.*
        backend:
          serviceName: navity-node
          servicePort: 8080
      - path: /static/.*
        backend:
          serviceName: navity-web-ui
          servicePort: 8080
      - path: /index.html.*
        backend:
          serviceName: navity-web-ui
          servicePort: 8080
      - path: /
        backend:
          serviceName: navity-web-ui
          servicePort: 8080
      - path: /.*
        backend:
          serviceName: navity-node
          servicePort: 8080
---